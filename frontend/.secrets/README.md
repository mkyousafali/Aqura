# Secrets Folder

⚠️ **NEVER commit files in this folder to Git.**

This folder is for storing sensitive credentials such as:
- `google-service-account.json` — Google Cloud service account key for Vertex AI (Veo, Imagen, Gemini, TTS)

The `.gitignore` in this folder excludes everything except itself and this README.
