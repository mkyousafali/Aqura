{
  "buildCommand": "pnpm --filter frontend build",
  "outputDirectory": "frontend/build",
  "installCommand": "pnpm install",
  "framework": null,
  "routes": [
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}