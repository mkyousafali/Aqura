{
  "buildCommand": "ls -la && pwd && cd frontend && pwd && pnpm install --ignore-scripts && pnpm build",
  "outputDirectory": "frontend/build",
  "installCommand": "echo 'Skipping root install'",
  "framework": null,
  "routes": [
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}