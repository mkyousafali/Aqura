# Dependencies
node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production

# Copilot instructions (contains server credentials)
.github/copilot-instructions.md

# Do not delete folder — may contain sensitive info, credentials, architecture docs
Do not delete/
scripts/Aqura-Branch-Installer/Install-AquraBranch.ps1

# Build output
frontend/build/

# Temporary SQL/script files (may contain credentials)
temp_*
test_branch_*

# Manual sync guide (contains server IPs, credentials, SSH setup)
scripts/MANUAL_SYNC_GUIDE.md

# API Keys and Secrets
*.key
**/OPENAI_API_KEY*
**/openai-api-key*
sk-proj-*
sk-*
**/sk-proj-*
VITE_OPENAI_API_KEY=sk-*
OPENAI_API_KEY=sk-*

# Security keys (VAPID keys, etc.)
VAPID_PRIVATE_KEY.txt
*.private.key
*.secret

# SSH keys and personal credentials
.ssh/
*.pem
id_rsa*
id_ed25519*

# Service configuration files with secrets
scripts/**/.env*
scripts/**/*.env
**/service-key.json

# Build outputs
.next/
out/
dist/
.output/
.nuxt/
.cache/
.parcel-cache/

# Electron app - keep local only (large size, not needed for Vercel)
erp-sync-app/
scripts/local-branch-sync/

# Aqura Desktop (Windows Electron shell) - local only, not part of PWA deploy
desktop/

# Desktop frontend - generated files (copied from web frontend via sync script)
desktop/frontend/src/routes/
desktop/frontend/src/lib/components/
desktop/frontend/src/lib/stores/
desktop/frontend/src/lib/utils/
desktop/frontend/src/lib/supabase*.ts
desktop/frontend/static/
desktop/frontend/.svelte-kit/
desktop/frontend/build/
desktop/frontend/node_modules/

# Desktop frontend - DO track these (desktop-specific)
!desktop/frontend/src/desktop/
!desktop/frontend/package.json
!desktop/frontend/vite.config.desktop.ts
!desktop/frontend/.env.desktop

# Aqura QR Kiosk (standalone break QR Electron app) - local only
qr-kiosk/

# Storage Sync Service (contains service keys in .env.local)
scripts/storage-sync-service/

# SvelteKit
.svelte-kit/
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Claude Code local settings - contains server IP/SSH command history, machine-specific
.claude/settings.local.json

# IDE files
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log

# Temporary files
*.tmp
*.temp

# Database
*.db
*.sqlite

# Go binaries
backend/main
backend/*.exe
backend/tmp/

# Test coverage
coverage/
*.cover
*.coverprofile

# Static assets (if auto-generated)
frontend/static/icons/create-favicon.ps1
frontend/generate-icons.ps1

# Backup files
*.bak
*.backup

# SSH keys (never commit)
*.pem
id_rsa*
id_ed25519*
authorized_keys
known_hosts

# Lock files (keep pnpm-lock.yaml for consistency)
package-lock.json
yarn.lock
.vercel

# Flyer Master standalone app (not part of Aqura)
Flyer Master/

# Build artifacts / archives (may contain embedded keys)
*.zip
*.tar.gz
*.rar
frontend/aqura-frontend-build.*

# Deployment pipeline temp files
aqura_dump.sql.gz
aqura_build.tar.gz
reset_sequences.sql
aqura-frontend-build.zip

# Deployment pipeline scripts & guides (local use only)
scripts/deploy-to-branch.ps1
Do not delete/BRANCH_DEPLOYMENT_GUIDE.md

# One-off utility scripts with key references
fix_key.py
replacements.txt
d e p l o y _ c l e a r _ a n a l y t i c s _ l o g s . s q l

 d e p l o y _ g e t _ a n a l y t i c s _ l o g _ t a b l e s . s q l


# Edge functions with hardcoded secrets (do NOT commit)
supabase/functions/ai-generate-poster/
supabase/functions/ai-poll-video/
supabase/migrations/01_full_schema.sql
supabase/migrations/02_functions_rpcs.sql
supabase/functions/ai-generate-video-start/
test_payload.json


# AQURA Desktop Development Plan (contains architecture, implementation details)
AQURA_DESKTOP_WINDOWS_DEVELOPMENT_PLAN.md
DESKTOP_ARCHITECTURE_DECISIONS.md

# Aqura Action Sync (ERP/printer sync agent) - local only, contains hardcoded Supabase keys
aqura-action-sync/

# Local-only files (not needed for app to work)
AQURA-LC/
aqura-lc-schema-dump.sql
aqura-ready-to-deploy/
scripts/prepare-deploy-bundle.ts

# ERP bridge setup scripts (branch-specific, contain shared secret placeholders)
scripts/erp-setup-wizard.js
scripts/erp-bridge-server.js

# Aqura Data Manager - standalone branch setup tool, local-only
scripts/Aqura-Data-Manager/

# ERP backfill/one-time data-repair scripts - inert local tools, not part of the running app
scripts/erp-backfill-dry-run.mjs
scripts/erp-backfill-write.mjs
scripts/backfill-erp-check-results.js

# Branch-server deployment script (not part of Aqura app, contains example credentials)
scripts/erp-bridge-server.js

# Aqura PC Lock Guard - entire folder is local-only (contains hardcoded service keys, local device/SQL data)
aqura-pc-lock-guard/

# Standalone designer app (local development only)
aqura-designer-standalone/

# Standalone Hikvision player app - contains hardcoded secrets, not for main repo
hikvision-player/

# Aqura ERP Password Manager - entire folder is local-only (views/rotates live
# ERP credentials per branch, hardcoded service keys, must never be committed)
Aqura Erp Psd Manager/

# Copilot worktree metadata - local workspace only
.claude/

.vercel

# Local npm cache artifact, not project files
.npm-cache/
