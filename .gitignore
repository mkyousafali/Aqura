# Dependencies
node_modules/
**/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.production

# Copilot instructions (contains server credentials)
.github/copilot-instructions.md

# AI guides with credentials/architecture details
Do not delete/AI_BRANCH_INSTALLER_AND_SYNC_GUIDE.md

# Build output
frontend/build/

# Temporary SQL/script files (may contain credentials)
temp_*
test_branch_*

# Manual sync guide (contains server IPs, credentials, SSH setup)
scripts/MANUAL_SYNC_GUIDE.md

# API Keys and Secrets
*.key
**/OPENAI_API_KEY*
**/openai-api-key*
sk-proj-*
sk-*
**/sk-proj-*
VITE_OPENAI_API_KEY=sk-*
OPENAI_API_KEY=sk-*

# Security keys (VAPID keys, etc.)
VAPID_PRIVATE_KEY.txt
*.private.key
*.secret

# Build outputs
.next/
out/
dist/
.output/
.nuxt/
.cache/
.parcel-cache/

# Electron app - keep local only (large size, not needed for Vercel)
erp-sync-app/
scripts/local-branch-sync/

# SvelteKit
.svelte-kit/
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# IDE files
.vscode/settings.json
.vscode/launch.json
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Logs
logs
*.log

# Temporary files
*.tmp
*.temp

# Database
*.db
*.sqlite

# Go binaries
backend/main
backend/*.exe
backend/tmp/

# Test coverage
coverage/
*.cover
*.coverprofile

# Static assets (if auto-generated)
frontend/static/icons/create-favicon.ps1
frontend/generate-icons.ps1

# Backup files
*.bak
*.backup

# SSH keys (never commit)
*.pem
id_rsa*
id_ed25519*
authorized_keys
known_hosts

# Lock files (keep pnpm-lock.yaml for consistency)
package-lock.json
yarn.lock
.vercel

# Flyer Master standalone app (not part of Aqura)
Flyer Master/

# Build artifacts / archives (may contain embedded keys)
*.zip
*.tar.gz
*.rar
frontend/aqura-frontend-build.*

# Deployment pipeline temp files (deploy-to-branch.ps1)
aqura_dump.sql.gz
aqura_build.tar.gz
reset_sequences.sql
aqura-frontend-build.zip

# One-off utility scripts with key references
fix_key.py
replacements.txt
