{
  "buildCommand": "cd frontend && pnpm install --ignore-scripts && pnpm build",
  "outputDirectory": "frontend/build",
  "installCommand": "echo 'Skipping root install'",
  "framework": null,
  "routes": [
    {
      "src": "/.*",
      "dest": "/index.html"
    }
  ]
}